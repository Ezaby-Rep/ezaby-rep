const passInput = document.querySelector('input[type=password]');
const
passInput.addEventListener('input', (e) => {
  console.log(e.target.value);
  if (e.target.value === 'islam2023') {
    window.location.replace('/tasks/فرش%20الفروع%20الجديدة')
  } else {

  }
})

